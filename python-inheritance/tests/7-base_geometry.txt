The ``BaseGeometry`` class provides a foundation for geometric shapes.

First, initialize the class and check the area exception:
    >>> BaseGeometry = __import__('7-base_geometry').BaseGeometry
    >>> bg = BaseGeometry()
    >>> try:
    ...     bg.area()
    ... except Exception as e:
    ...     print(e)
    area() is not implemented

Test valid integer validation:
    >>> bg.integer_validator("age", 1)

Test integer validation with 0 (should raise ValueError):
    >>> try:
    ...     bg.integer_validator("age", 0)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

Test integer validation with negative number (should raise ValueError):
    >>> try:
    ...     bg.integer_validator("age", -1)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

Test integer validation with a string (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", "4")
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer validation with a tuple (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", (4,))
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer validation with a list (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", [3])
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer validation with a boolean True (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", True)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer validation with a list of integers (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", [3, 4])
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer validation with None (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", None)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer
